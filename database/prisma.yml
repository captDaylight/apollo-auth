endpoint: https://eu1.prisma.sh/paulchristophe6-1adc8b/travel/dev
datamodel: datamodel.graphql
secret: ${env:APP_SECRET}

# Deploy hook
hooks:
  post-deploy:
    - graphql get-schema --project database
