# import Trip from "./generated/prisma.graphql"

type Query {
  trips: [Trip!]!
}

type Mutation {
  createTrip(name: String!, description: String!): Trip!
}

type User {
  id: ID! @unique
  email: String! @unique
  name: String
  trips: [Trip!]!
}
